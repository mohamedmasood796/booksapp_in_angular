import { NriCurrencyPipe } from './nri-currency.pipe';

describe('NriCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new NriCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
